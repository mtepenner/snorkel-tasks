Milestone 3 is replay export. Use ImageMagick (Magick++ or CLI), write replay PNGs to /app/workspace/data/replays/, and create that folder first if needed.

Metadata is required and the key has to be exactly Game-Metadata. Include useful game context in the value, for example board state, move list, mine count, or status.
