Build the app in `/app/index.html` as a single client-side HTML file, loading React, ReactDOM, Babel, and a PDF parser like pdf.js from the provided `/app/vendor/` directory. The page must include a visible file picker for `.md`, `.markdown`, and `.pdf` files, accompanied by a status message. The analysis must run automatically as soon as a file is loaded. Do not add a server, API, or background worker. Do not require an extra submit or analyze button after file selection. Do not rename fields, omit required keys, or replace the JSON mirror with formatted prose.

For every upload, read the raw document text and compute the analysis deterministically. Count bracket citations such as `[1]`, and compute `totalWords` using `/\b[\w'-]+\b/g` so heading words and citation numbers count. Under `keywordDensity`, report `quantum` and `entanglement` case-insensitively with `count` and `density` (`count / totalWords` rounded to 3 decimals). For Markdown, treat lines starting with `#` as headings and summarize each with the first non-empty sentence. For PDFs, extract text client-side, preserve line breaks, and treat short standalone title-case lines (roughly 1 to 4 words of letters, numbers, spaces, or hyphens) as headings to recover sections.

Render dashboard sections titled exactly `Citation Frequency`, `Keyword Density`, and `Section Summaries`, placing the summaries inside a `.sections` container with each section title in a `<strong>` element so they are easy to scan. Render a machine-readable JSON mirror of the current analysis in a `<pre id="analysis-output"></pre>`. The JSON must use the exact field names `fileType`, `citations`, `totalWords`, `keywordDensity`, and `sectionSummaries` (where `sectionSummaries` is an array of objects, each containing a `title` and a `summary` key). Upon successful PDF parsing, update the mirror with `fileType: "pdf"` and the parsed counts, ensuring the initial empty state is cleared and the output accurately reflects only the explicitly found section titles. For Markdown files (including `.md` and `.markdown`), ensure the `fileType` field in the JSON is set specifically to `"markdown"`.
