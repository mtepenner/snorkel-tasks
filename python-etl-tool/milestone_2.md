# Frontend Dashboard

Need the dashboard page at `/app/workspace/src/templates/index.html`. Add a button that sends a `POST` request to `/trigger`, disables itself while the request is in flight, and turns back on when the request finishes. Add a `<pre>` block that fetches and shows `/logs` on page load, and keep a download link pointing at `/download`.
