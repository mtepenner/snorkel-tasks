For this first milestone, please just focus on the backend processing. create the flask app in `/app/workspace/src/app.py` running on port 5000. need POST `/trigger` to run the actual etl (read the csvs from `/app/workspace/data/input/`, convert, dump to `/app/workspace/data/output.json`, and insert into `records` table in `/app/workspace/data/etl.db`). set up GET `/logs` and GET `/download` routes too. make sure the script appends timestamped success/error states to `/app/workspace/data/etl.log` every run. use the schema CREATE TABLE records (id INTEGER PRIMARY KEY, data TEXT) where the data column stores the entire csv row as a json string.