# [CORE-101] Create text file

**Description:**
Need a quick `hello.txt` file dropped in the container for basic health checks. Do it ASAP.

**AC:**
* Put it exactly at `/app/hello.txt`
* Contents must be exactly `Hello, world!`

That's it, just push it so the build turns green.
