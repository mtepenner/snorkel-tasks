milestone 2: toss a simple responsive web gui into `/app/workspace/src/templates/index.html` (put any static js you need in `/app/workspace/src/static/js/app.js`) and have the c++ server return it from `GET /`.
- build a basic dashboard layout. needs a `<nav>` or sidebar with links/text for "data", "model", and "inference".
- make one model config form. put the `<select>` dropdown with at least two options and the hyperparameter slider (`<input type="range">`) inside that same form.
- throw in a basic chart area (a `<canvas>` or `<svg>`) to show dataset distributions.
- keep it responsive (include a viewport meta tag).

