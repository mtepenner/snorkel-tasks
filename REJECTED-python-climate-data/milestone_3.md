# Graph Output

Need `/app/workspace/output/climate_graph.gv` and `/app/workspace/output/climate_graph.png`. Make it a directed Graphviz graph with edges from each dynamic region to its mean temperature, and keep decimal temperature node text intact in the raw DOT source instead of flattening it.
