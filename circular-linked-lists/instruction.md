hey there! we're helping the travel desk ditch their manual tracking by building a new c++17 command-line tool, and i need you to put the main program entry point at /app/workspace/src/travel_groups.cpp. you can absolutely add small helper files in that same folder since the test harness compiles all /app/workspace/src/*.cpp files, but everything must compile flawlessly with zero warnings using exactly g++ -std=c++17 -wall -wextra -pedantic. the architectural backbone of the program needs to be an array containing exactly three genuine circular linked lists, strictly ordered as scotland, hamburg, and moscow. you have to build real node-based structures with actual next pointers that wrap around properly—please don't use std::map or try to fake the ring behavior with dynamic arrays or vectors. each node represents a single itinerary and must store a unique booking_id, the traveler_name, the group name, the number of nights (which must be a strictly positive integer), and a transport method that is strictly limited to either flight, train, or ferry.

the program should read its commands from stdin, splitting the input on the | character, and it needs to completely ignore blank lines or any lines starting with a #. when a book|booking_id|traveler_name|group|nights|transport request comes in, append it to the tail of the specified group's ring and print BOOKED <booking_id> GROUP <group>, but make sure to catch bad data first: print ERROR duplicate booking <booking_id> if the id already exists anywhere in the structure, or output ERROR invalid group <group>, ERROR invalid nights <nights>, or ERROR invalid transport <transport> if those fields break the rules. each group tracks a "current" pointer alongside its ring; the very first booking becomes the current node, while later bookings just append to the tail without changing that pointer. to move around, an advance|group|steps command walks the current pointer forward by the given steps (wrapping around as needed) and prints CURRENT <group> <booking_id> <traveler_name>, or just EMPTY <group> if the ring has no nodes. you will also need a show|booking_id command that returns MISSING <booking_id> or exactly FOUND <booking_id> | traveler=<traveler_name> | group=<group> | nights=<nights> | transport=<transport>. finally, a cancel|booking_id request removes the node from whichever group holds it and prints either CANCELLED <booking_id> GROUP <group> or MISSING <booking_id>; if you delete the current node, the pointer must step back to the previous node in the ring (or become EMPTY if it was the only node), but if you delete a non-current node, the current pointer stays exactly where it was.

to wrap things up, we need two specific reporting commands to summarize the system's state. the GROUP_REPORT command must print exactly three lines, strictly following the array order of the groups: GROUP scotland=<count> CURRENT=<booking_id_or_EMPTY>, followed by the exact same formatted lines for hamburg and moscow. then, the TRANSPORT_REPORT command needs to output a five-line summary in this exact order: TOTAL_BOOKINGS=<total>, SCOTLAND_FAVORITE_TRANSPORT=<transport_or_NONE>, HAMBURG_FAVORITE_TRANSPORT=<transport_or_NONE>, MOSCOW_FAVORITE_TRANSPORT=<transport_or_NONE>, and OVERALL_FAVORITE_TRANSPORT=<transport_or_NONE>. whenever you are calculating the "favorite" transport, simply find the most-booked transport method within that specific scope, and if there is ever a tie between two or more methods, always break the tie alphabetically.
