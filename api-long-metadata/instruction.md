i am handing off the pdf extraction script. put api at /app/workspace/src/api.py on port 8000. documents are huge so you have to implement text chunking. 1000 words max per chunk. Create an endpoint at POST /extract. Accept a pdf upload via multipart/form-data under the field 'file'. pull author and title directly from the pdf document metadata properties. return Unknown Author or Untitled if those explicit metadata fields are missing. then read the actual text chunks to extract the topics. frontend needs a specific json dictionary back. keys must be exactly: author (string), title (string), topics (array of strings), total_chunks (integer), filename (string), total_words (integer). make quick UI to test it. put index.html in /app/workspace/src/static. add upload button, hit the api, and print the json to the screen.
