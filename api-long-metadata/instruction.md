I am handing off the PDF extraction script. Put the api at /app/workspace/src/api.py on port 8000. Documents are huge so you have to implement text chunking. 1000 words max per chunk. Create an endpoint at POST /extract. Accept a pdf upload via multipart/form-data under the field 'file'. Read the chunks to find the author, title, and topics. Return Unknown Author or Untitled if they are missing. The frontend needs a specific json dictionary back. Keys must be exactly: author (string), title (string), topics (array of strings), total_chunks (integer), filename (string), total_words (integer). Make a quick UI to test it. Put an index.html in /app/workspace/src/static. Add an upload button, hit the api, and print the json to the screen.