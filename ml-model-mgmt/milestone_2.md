milestone 2: toss a simple responsive web gui into `/app/workspace/src/templates/index.html` (put any static js you need in `/app/workspace/src/static/js/app.js`).
- build a basic dashboard layout. needs a `<nav>` or sidebar with links/text for "data", "model", and "inference".
- make a config form so users can pick a dummy model. needs a `<select>` dropdown with at least two options, and a slider (`<input type="range">`) for hyperparameters.
- throw in a basic chart area (a `<canvas>` or `<svg>`) to show dataset distributions.
- keep it responsive (include a viewport meta tag).

